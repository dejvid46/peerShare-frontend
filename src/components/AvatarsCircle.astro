---
import AvatarPosition from "../react/AvatarPosition";

const count = [...Array(2).keys()].map(x => x+1);
---

<div>
    {count.map(x => <AvatarPosition id={Math.floor(Math.random() * 10000000000000)} order={x} count={count[count.length-1]} client:visible/>)}
</div>
